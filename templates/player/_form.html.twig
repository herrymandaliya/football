{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}



{{ form_start(form) }}
{{ form_errors(form) }}
<div class="mb-3">
  <label class="form-label" for="project_cost">Project Name</label>
  {{ form_row(form.name, {'attr': {'class': 'form-control'} , label: false })  }}
</div>
<div class="mb-3">
  <label class="form-label" for="project_cost">Cost</label>
  {{ form_row(form.cost, {'attr': {'class': 'form-control'} , label: false })  }}
</div>
<div class="mb-3 col-4">
  {{ form_row(form.start_date.month, {'attr': {'class': 'form-select'} , label: false })  }}
</div>
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
